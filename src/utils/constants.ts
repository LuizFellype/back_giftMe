export const INFOS = {
  USER: `{ id name email password recognizeId products { id productName url } partner { name email recognizeId products { id productName url } } }`,
  PARTNER_RECONGIZEID: `{ partner { recognizeId } }`,
  ADDPARNTNER: `{ partner { name recognizeId products { id productName url } } }`
}
